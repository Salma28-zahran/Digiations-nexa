import 'package:flutter/material.dart';

class HomeManger extends StatelessWidget {
  const HomeManger({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
